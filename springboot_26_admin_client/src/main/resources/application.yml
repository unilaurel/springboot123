spring:
  boot:
    admin:
      client:
        url: http://localhost:8080

server:
  port: 80
management:
  endpoint:
    health:
      show-details: always

  endpoints:
    web:
      exposure:
        include: "*"
    enabled-by-default: true

info:
  appName: @project.artifactId@
  version: @project.version@
  company: ????
  author: itheima
